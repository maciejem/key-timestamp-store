spring:
  datasource:
    url: jdbc:postgresql://db:5432/kvstore
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  tomcat:
    virtual-threads: true
